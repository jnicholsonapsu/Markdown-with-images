# Markdown with images

## This is an example of using Markdown

This project contains an example of using images with markdown. Go into the folder `example` and view the `example.md` file. View [example/example.md](example/example.md)

This is a quick example of using markdown and Visual Studio Code.
